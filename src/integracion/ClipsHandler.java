package integracion;

/*import net.sf.clipsrules.jni.Environment;
import net.sf.clipsrules.jni.PrimitiveValue;

public class ClipsHandler {

	private Environment clips;
	private static ClipsHandler handler;
	private static final String file = "clips/reglas.CLP";
	
	public enum Metodologia { RCP, BLABLA };
	
	private ClipsHandler() {
		this.clips = new Environment();
		
	}
	
	public static ClipsHandler instance() {
		if(handler == null) {
			handler = new ClipsHandler();
		}
		return handler;
	}
	
	public Metodologia run() {
		this.clips.run();
		
		PrimitiveValue respuesta = this.clips.eval("(find-all-facts (?val WTF) TRUE)");
		
		// TODO Hacer algo con la respuesta;
		
		
		this.clips.reset();
		
		// TODO Acordarse de borrar esto...
		return Metodologia.RCP;
	}
	
}*/
